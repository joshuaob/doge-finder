<% if @doge.present? %>
  <%= turbo_stream.update "flash" %>
  <%= turbo_stream.replace "doge", partial: "doge", locals: { dodge: @doge } %>
<% else %>
  <%= turbo_stream.replace "flash", partial: "layouts/flash", locals: { message: flash[:notice] } %>
<% end %>
